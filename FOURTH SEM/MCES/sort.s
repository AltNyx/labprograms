	AREA CODE1, CODE, READONLY
ENTRY
	LDR R0, =INPUT
	LDMIA R0, {R1-R10}
	LDR R0, =SORT
	STMIA R0, {R1-R10}
	MOV R2, #9; i = 9
OUTER LDR R0, =SORT
	MOV R3, R2; j = i
REPEAT LDR R4, [R0], #4
	   LDR R5, [R0]
	   CMP R4, R5
	   BLE SKIP
	   SWP R5, R4, [R0]
	   SUB R0, R0, #4
	   SWP R4, R5, [R0]
	   ADD R0, R0, #4
SKIP SUB R3, #1
	 CMP R3, #0
	 BNE REPEAT
	 SUB R2, #1
	 CMP R2, #0
	 BNE OUTER
STOP B STOP
INPUT DCD 6, 7, 5, 8, 9, 4, 1, 2, 3, 10
	AREA DATA1, DATA, READWRITE
SORT SPACE 40
	END